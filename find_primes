def initial():
  primes=[2]
  def undefined_func(z):
    counter = primes[-1]
    print(primes, counter)
    def test_primes(val):
      for num in primes:
        if val%num == 0:
          return
      primes.append(val)
      return
    while counter<z:
      test_primes(counter)
      counter +=1
    return primes
  return undefined_func

find_primes = initial()
